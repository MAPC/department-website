.ui.card
  = link_to staff, class: 'image' do
    %img{src: staff.avatar.url(:thumbnail), class: ''}
  .content
    = staff.name
    .description
      = staff.role
  = mail_to staff.email, class: 'ui bottom attached basic button' do
    %i.mail.icon
    Contact
